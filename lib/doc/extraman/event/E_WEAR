Event:     E_WEAR

Called:    Whenever a living object wears an item.

Usage:     Add an event (see add_event), create an event handling function, 
           that will be called if the event occurs:
	   int function(object tp, object ob) { ... }

	   if the function returns an integer != 0, wearing is forbidden.

Arguments: object tp - the living that wears the object
           object ob - the item to wear
